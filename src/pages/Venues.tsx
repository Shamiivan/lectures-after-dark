import { BarsHeader, BarsInfo, BarsList, BarsCTA } from '../components/Bars';

const Venues = () => {
    return (
        <div>
            <BarsHeader />
            <BarsInfo />
            <BarsList />
            <BarsCTA />
        </div>
    );
};

export default Venues;
